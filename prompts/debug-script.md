# Prompt — Debug ABBYY Script

Use `docs/errors.md` and the relevant official object-model files.

Input:

```text
[ERROR MESSAGE]
```

Script:

```javascript
[PASTE SCRIPT]
```

Return:

1. Exact failing expression
2. Most likely root cause
3. How to verify the cause
4. Minimal safe correction
5. Required rule configuration changes
6. Regression test cases
